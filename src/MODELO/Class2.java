package MODELO;

public class Class2 {
    /*public  String[] procesarNombres(int tamaño, String[] nombres){
        String[] = Longitudes = new String(tamaño);
        for (int i = 0 ; i < tamaño;i++){
            longitudes[i] = String.ValueOf(nombres)
        }
    }
    */
    public int ObtengoLongitud(String nombre) {
        return nombre.length();
    }
}
